# IA — OpenRouter (motor del cerebro de Hermes Core)
OPENROUTER_API_KEY=sk-or-v1-tu-api-key

# Base de datos — Neon Postgres (memoria + persistencia)
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Metadata
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Hermes Command Center
